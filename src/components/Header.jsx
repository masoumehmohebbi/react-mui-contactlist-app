import React from "react";
import Stack from "@mui/material/Stack";
import Typography from "@mui/material/Typography";

const Header = () => {
  return (
    <Stack spacing={2}>
      <Typography>Hearder</Typography>
    </Stack>
  );
};

export default Header;
